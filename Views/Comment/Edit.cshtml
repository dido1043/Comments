@using Comments.Models.ViewModels
@model FormViewModel

<form asp-action="Edit" method="post">
    <div class="mb-3">
        <label asp-for="@Model.Text" class="form-label-add-edit">Seminar Topic</label>
        <input asp-for="@Model.Text" class="form-control" aria-required="true" />
        <span asp-validation-for="@Model.Text" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <input class="btn btn-primary mb-2 w-100 p-3 fw-bold" style="margin-top = 30px" type="submit" value="Споделяне" />
    </div>
</form>